#!/usr/bin/env ruby

require 'floadr'

flickr = Flickr.new
flickr.initialize_api_key()
photo_id, _ = flickr.get_most_interesting_photo("space")
photo_url = flickr.get_photo_url(photo_id)
puts photo_url
